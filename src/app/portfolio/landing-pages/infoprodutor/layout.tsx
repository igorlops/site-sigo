import type { Metadata } from "next"; import { Sora } from "next/font/google"; const f = Sora({ subsets: ["latin"] }); export const metadata: Metadata = { title: "ExpertLaunch | Transforme Conhecimento em Renda", description: "Crie, lance e venda seu curso online. Plataforma completa para infoprodutores com suporte 360°." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
