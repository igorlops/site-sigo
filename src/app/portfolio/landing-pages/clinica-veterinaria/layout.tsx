import type { Metadata } from "next"; import { Quicksand } from "next/font/google"; const f = Quicksand({ subsets: ["latin"] }); export const metadata: Metadata = { title: "VetCare+ | Clínica Veterinária 24h", description: "Atendimento veterinário completo. Emergências 24h, cirurgias, exames laboratoriais e internação." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
