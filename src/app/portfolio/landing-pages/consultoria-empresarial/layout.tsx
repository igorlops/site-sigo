import type { Metadata } from "next"; import { IBM_Plex_Sans } from "next/font/google"; const f = IBM_Plex_Sans({ subsets: ["latin"], weight: ["400", "600", "700"] }); export const metadata: Metadata = { title: "Stratégia Consultoria | Transformação de Negócios", description: "Consultoria estratégica para PMEs e grandes empresas. Implementamos mudanças que geram resultado." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
