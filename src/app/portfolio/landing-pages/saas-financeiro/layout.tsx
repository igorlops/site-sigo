import type { Metadata } from "next"; import { Inter } from "next/font/google"; const font = Inter({ subsets: ["latin"] }); export const metadata: Metadata = { title: "FinanceAI | Gestão Financeira Inteligente", description: "Plataforma SaaS para controle financeiro empresarial. DRE automatizado, fluxo de caixa e BI em tempo real." }; export default function SaaSFinanceLayout({ children }: { children: React.ReactNode }) { return <div className={font.className}>{children}</div>; }
