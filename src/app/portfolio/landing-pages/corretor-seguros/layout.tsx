import type { Metadata } from "next"; import { Poppins } from "next/font/google"; const font = Poppins({ subsets: ["latin"], weight: ["400", "600", "700"] }); export const metadata: Metadata = { title: "SeguriTech Corretora | Seguros Personalizados", description: "Seguros de vida, saúde, auto e empresarial com atendimento humanizado e tecnologia. Simulação online em 2 minutos." }; export default function SeguroLayout({ children }: { children: React.ReactNode }) { return <div className={font.className}>{children}</div>; }
