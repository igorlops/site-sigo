import type { Metadata } from "next"; import { Fredoka } from "next/font/google"; const f = Fredoka({ subsets: ["latin"] }); export const metadata: Metadata = { title: "PawsLuxe | Pet Shop Premium & Banho e Tosa", description: "Produtos importados, rações premium, banho e tosa com SPA. Tudo que seu pet de estimação merece." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
