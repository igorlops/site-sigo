import type { Metadata } from "next"; import { Poppins } from "next/font/google"; const f = Poppins({ subsets: ["latin"], weight: ["400", "600", "700"] }); export const metadata: Metadata = { title: "DevMaster Academy | Cursos de Programação Online", description: "Aprenda programação do zero ao avançado. Python, JavaScript, React e mais. Certificado reconhecido pelo mercado." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
