import type { Metadata } from "next"; import { Space_Grotesk } from "next/font/google"; const f = Space_Grotesk({ subsets: ["latin"] }); export const metadata: Metadata = { title: "NicheStore | Ecommerce de Produtos Artesanais", description: "Curadoria de produtos únicos e artesanais. Compre direto dos criadores com frete grátis." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
