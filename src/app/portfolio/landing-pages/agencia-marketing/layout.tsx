import type { Metadata } from "next"; import { Outfit } from "next/font/google"; const f = Outfit({ subsets: ["latin"] }); export const metadata: Metadata = { title: "Pixel Storm | Agência de Marketing Digital 360°", description: "Performance, branding e growth. Transformamos cliques em clientes através de estratégias data-driven." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
