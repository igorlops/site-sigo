import type { Metadata } from "next"; import { Nunito } from "next/font/google"; const f = Nunito({ subsets: ["latin"] }); export const metadata: Metadata = { title: "SpeakWorld | Inglês e Espanhol Fluente", description: "Escola de idiomas com metodologia imersiva. Turmas pequenas, professores nativos e certificação internacional." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
