import type { Metadata } from "next"; import { Inter } from "next/font/google"; const f = Inter({ subsets: ["latin"] }); export const metadata: Metadata = { title: "SalesHub CRM | Vendas que Escalam", description: "CRM completo para equipes comerciais. Pipeline visual, automação de follow-up e integrações nativas." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
