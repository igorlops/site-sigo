import type { Metadata } from "next"; import { Plus_Jakarta_Sans } from "next/font/google"; const f = Plus_Jakarta_Sans({ subsets: ["latin"] }); export const metadata: Metadata = { title: "Quantum Labs | Startup de Tecnologia Disruptiva", description: "Desenvolvemos soluções SaaS que escalam. MVP em 90 dias, infraestrutura serverless e captação de investimento." }; export default function L({ children }: { children: React.ReactNode }) { return <div className={f.className}>{children}</div>; }
